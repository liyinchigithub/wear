<shape xmlns:android="http://schemas.android.com/apk/res/android" >
    <stroke
        android:width="2dp"
        android:color="@color/colorPrimary"
        android:dashWidth="4dp"
        android:dashGap="2dp" />
    <solid android:color="@color/colorPrimary"/> <!-- 滑块的颜色 -->
    <corners android:radius="5dp"/> <!-- 滑块的圆角大小 -->
</shape>